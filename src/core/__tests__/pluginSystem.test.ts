import { runHook } from '../pluginSystem';

describe('pluginSystem', () => {
  it('should run onPlan and onGenerate hooks if plugins are present', () => {
    // This is a placeholder test. In a real scenario, you would mock require and config loading.
    expect(typeof runHook).toBe('function');
  });
});
